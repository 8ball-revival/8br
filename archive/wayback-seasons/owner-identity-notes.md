# Owner-supplied identity facts

Handles the archive prints that the sources alone cannot resolve. Everything here came from the
owner directly; none of it is inferred from the pages.

Nothing in this file has been applied. It is the input to the identity step that runs before these
Seasons import — recorded here so the decisions do not have to be made again from memory.

## Same person, more than one handle

| Handle | Is | Also used |
|---|---|---|
| `sykology` | TrueBoston | |
| `cocky_guy` | Craig | `I_am_aimost_god` |
| `jmunoz3rd` | Jesse | |
| `x_i_am_me` / `x_i_am_me_x` | Alex | |
| `King_jLuis` | neo | `starkiller` |
| `shadow_man_` | neo | `starkiller` |
| `xlx_k_e_t_a_n_xlx` | | `l_ketan_l` |

`King_jLuis` and `shadow_man_` both resolve to neo, which makes them one person holding two handles
rather than two spellings of one. That is a merge, and merges are not made without the owner saying
so for that specific pair — so it is written down here and left alone.

## Why a bracket names people the group stage does not

The owner's account: players changed their CueVerse ID mid-Season, and the admins updated the
playoff bracket without going back to the groups. So a handle can hold an entry position on a
bracket and be absent from the same Season's participant list, without anybody having entered late.

Their rule of thumb is that the new ID usually belongs to the highest-seeded qualifier who is
otherwise missing from the draw. That is a good place to start looking and not a basis for writing
a record: applied automatically it would silently reassign matches to the wrong person. The step it
feeds is a report — each unmatched bracket handle beside the excluded qualifiers ranked by seed —
for the owner to confirm one at a time.

## Spellings already settled

These were not identity decisions. Each is one page spelling one person two ways, resolved by the
transcriber to whichever spelling that page uses most, and listed here only so the substitution is
visible outside the tool that made it.

- 2012 S2A — `lvlr.l3rutal_king` read as `lvlr_.l3utal_king`
- 2012 S3A — `nishilicious` read as `nichilicious`
- 2013 S1A — `l00%good` read as `100%good`
- 2013 S2A — `x_i_am_me` read as `x_i_am_me_x`
- 2013 S5A — `King_jLuis` read as `KingjLuis`

## Applied

`King_jLuis`, `KingjLuis`, `Kingj_Luis` and `shadow_man_` are now aliases of **Starkiller**
(`cmsys8lj000016rigqrgtm4qb`, 13 entrants), which the owner named canonical for neo. That account
already held `starkiller`, `cockyneo`, `poolkingneo` and `xgodneox` — so 2012 S2A's champion,
xGodNeox, is this same person.

## Open, not acted on

**`trustneo`** is a separate Player with four entrants. The name suggests the same person and the
owner has not mentioned it, so it stays separate. It is a merge if it is anybody.

**`iamalmostgod` is on two people.** Starkiller already carries it as an alias. The owner separately
said `cocky_guy` is Craig, who "also used I_am_aimost_god". Both cannot be right, and whichever way
it resolves it moves matches between two players. Nothing should read that handle until it is
settled — note also that Starkiller carries `cockyneo`, so the two identities may already be
entangled from an earlier pass rather than only in the sources.
