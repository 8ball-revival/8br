# Import Readiness Report

> Generated by `npm run archive:report` from the read-only 8BRCAM archive. Preview/staging only — nothing has been imported to the database.

**No records are imported in this phase.** `archive:import` is dry-run only.

| Metric | Value |
|---|---|
| Genuine schema blockers | 0 |
| High-severity issues | 532 |
| Items requiring review | 924 |
| Ready for a reviewed import | yes |

## Review queue by type

| Key | Count |
|---|---|
| shared-alias | 407 |
| name-duplicate | 278 |
| competition | 6 |
| match | 143 |
| data-completeness | 1 |
| championship | 90 |

## Manual decisions required before real import

1. Resolve **shared aliases** (407) — merge or split each ambiguous identity.
2. Review **merge candidates** (0) and **name-only duplicates** (278) — do not merge on name alone.
3. Decide handling of **non-explicit championships** (90) — keep as heuristic/unknown or verify.
4. Review **impossible/forfeit-unclear matches** (143).
5. Confirm the **EGO Season 1 seeding annotation** stays an administrative note (not a results correction).
6. Choose an **entry method** default for archive `CompetitionEntry` rows (currently null/unknown).

## Blockers

None. The existing Prisma schema can represent the staged entities.
